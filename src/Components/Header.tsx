import * as React from 'react';
import { Box, AppBar, Typography } from '@mui/material';

export default function Header() {
  return (
    <div>
      <Box>
        <AppBar>
          <Typography>
            <h1> take of experice </h1>
          </Typography>
        </AppBar>
      </Box>
    </div>
  );
}
